{
  try {
    workgroup.getProperties().setProperty("chatbot.session.timeout",String.valueOf(timeout));
  }
 catch (  UnauthorizedException e) {
    ComponentManagerFactory.getComponentManager().getLog().error("Error setting timeout",e);
  }
}
