{
  if (startTime != null) {
    long start=startTime.getTime();
    long end=endTime != null ? endTime.getTime() : 0;
    if (end == 0) {
      return new Date().getTime() - start;
    }
 else {
      return end - start;
    }
  }
  return 0;
}
