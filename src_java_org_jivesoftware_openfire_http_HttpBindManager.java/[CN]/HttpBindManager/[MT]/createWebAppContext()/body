{
  WebAppContext context=new WebAppContext(contexts,JiveGlobals.getHomeDirectory() + File.separator + "resources"+ File.separator+ "spank","/");
  context.setWelcomeFiles(new String[]{"index.html"});
  return context;
}
