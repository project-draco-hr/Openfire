{
  list.mark();
  for (int i=0; i < 3; i++) {
    assertEquals(0,list.getPassCount());
    list.next();
  }
  assertEquals(1,list.getPassCount());
  list.mark();
  for (int i=0; i < 10; i++) {
    list.next();
  }
  assertEquals(3,list.getPassCount());
}
