{
  JitsiMeetConference conference=conferences.get(roomName);
  if (conference == null) {
    logger.error("Auth request - no active conference for room: " + roomName);
    return;
  }
  conference.userAuthenticated(realJid,identity);
}
