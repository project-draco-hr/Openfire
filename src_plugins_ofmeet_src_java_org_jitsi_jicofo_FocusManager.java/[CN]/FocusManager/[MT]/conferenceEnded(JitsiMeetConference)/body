{
  String roomName=conference.getRoomName();
  conferences.remove(roomName);
  logger.info("Disposed conference for room: " + roomName + " conference count: "+ conferences.size());
  maybeDoShutdown();
}
