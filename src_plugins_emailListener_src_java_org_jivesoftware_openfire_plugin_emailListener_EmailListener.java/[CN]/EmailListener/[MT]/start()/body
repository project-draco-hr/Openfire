{
  if (started) {
    return;
  }
  Thread thread=new Thread("Email Listener Thread"){
    public void run(){
      folder=openFolder();
      if (folder != null) {
        listenMessages();
      }
    }
  }
;
  thread.setDaemon(true);
  thread.start();
  started=true;
}
