{
  if (isClosed()) {
    deliverer.deliver(packet);
  }
 else {
synchronized (writer) {
      try {
        xmlSerializer.flush();
      }
 catch (      IOException e) {
        Log.error(e);
      }
    }
    auditor.audit(packet);
    session.incrementServerPacketCount();
  }
}
