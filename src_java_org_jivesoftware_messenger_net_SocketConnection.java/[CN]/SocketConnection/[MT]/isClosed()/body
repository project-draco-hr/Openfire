{
  if (session == null) {
    try {
      sock.getInputStream();
    }
 catch (    IOException e) {
      return false;
    }
    return true;
  }
  return session.getStatus() == Session.STATUS_CLOSED;
}
