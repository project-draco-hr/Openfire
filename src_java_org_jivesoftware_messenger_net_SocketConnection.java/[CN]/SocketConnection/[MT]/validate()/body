{
  if (isClosed()) {
    return false;
  }
  try {
synchronized (writer) {
      xmlSerializer.writeCharacters(" ");
      xmlSerializer.flush();
    }
  }
 catch (  Exception e) {
    close();
  }
  return !isClosed();
}
