{
  try {
    setType(Type.valueOf(typeName));
  }
 catch (  IllegalArgumentException ie) {
    if (parent != null) {
      setType(Type.defaulType);
    }
 else {
      setType(Type.number);
    }
  }
}
