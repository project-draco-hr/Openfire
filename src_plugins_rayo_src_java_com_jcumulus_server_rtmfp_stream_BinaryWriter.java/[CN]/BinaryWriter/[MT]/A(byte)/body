{
  if (A < B) {
    E[A]=byte0;
    A++;
    if (A > D)     D=A;
  }
 else {
    C.warn("Limit is exceeded");
    try {
      throw new Exception();
    }
 catch (    Exception exception) {
      C.error("Trace - ",exception);
    }
  }
}
