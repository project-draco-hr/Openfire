{
  byte abyte0[]=BigInteger.valueOf(i).toByteArray();
  for (int k=0; k < j - abyte0.length; k++)   A((byte)0);
  byte abyte1[]=abyte0;
  int l=abyte1.length;
  for (int i1=0; i1 < l; i1++) {
    byte byte0=abyte1[i1];
    A(byte0);
  }
}
