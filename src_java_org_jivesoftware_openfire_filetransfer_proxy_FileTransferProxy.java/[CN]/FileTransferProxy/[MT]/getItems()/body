{
  if (!isEnabled()) {
    return Collections.emptyIterator();
  }
  final DiscoServerItem item=new DiscoServerItem(new JID(getServiceDomain()),"Socks 5 Bytestreams Proxy",null,null,this,this);
  return Collections.singleton(item).iterator();
}
