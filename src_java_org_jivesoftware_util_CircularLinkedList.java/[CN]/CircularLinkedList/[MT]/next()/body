{
  Object item=null;
  if (current != null) {
    currentLock.writeLock().lock();
    try {
      current=current.next;
      item=current.object;
    }
  finally {
      currentLock.writeLock().unlock();
    }
  }
  if (mark == current) {
    passCount++;
  }
  return item;
}
