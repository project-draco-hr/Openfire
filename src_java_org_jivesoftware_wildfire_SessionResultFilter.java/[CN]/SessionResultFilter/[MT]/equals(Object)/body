{
  if (this == object) {
    return true;
  }
  if (object != null && object instanceof SessionResultFilter) {
    SessionResultFilter o=(SessionResultFilter)object;
    return sortField == o.sortField && sortOrder == o.sortOrder && startIndex == o.startIndex && numResults == o.numResults && username == o.username && ((creationDateRangeMin == null && o.creationDateRangeMin == null) || creationDateRangeMin.equals(o.creationDateRangeMin)) && ((creationDateRangeMax == null && o.creationDateRangeMax == null) || creationDateRangeMax.equals(o.creationDateRangeMax)) && ((lastActivityDateRangeMin == null && o.lastActivityDateRangeMin == null) || lastActivityDateRangeMin.equals(o.lastActivityDateRangeMin)) && ((lastActivityDateRangeMax == null && o.lastActivityDateRangeMax == null) || lastActivityDateRangeMax.equals(o.lastActivityDateRangeMax));
  }
 else {
    return false;
  }
}
