{
  for (int i=0; i < connections.length; i++) {
    if (connections[i] != null && connections[i].getRequestId() == request.getRequestId()) {
      connections[i]=null;
      lastRequestId=request.getRequestId();
      return;
    }
  }
}
