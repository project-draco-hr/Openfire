{
  StatisticsManager.getInstance().addStatistic(USERS_CREATED_STAT,new i18nStatistic(USERS_CREATED_STAT,Statistic.Type.rate_total){
    public double sample(){
      return usersCreated.getAndSet(0);
    }
  }
);
}
