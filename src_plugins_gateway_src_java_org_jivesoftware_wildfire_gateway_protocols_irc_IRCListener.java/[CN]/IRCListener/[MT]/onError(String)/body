{
  Log.debug("IRC error: " + string);
  if (silenced) {
    return;
  }
  getSession().getTransport().sendMessage(getSession().getJID(),getSession().getTransport().getJID(),"IRC error received: " + string,Message.Type.error);
}
