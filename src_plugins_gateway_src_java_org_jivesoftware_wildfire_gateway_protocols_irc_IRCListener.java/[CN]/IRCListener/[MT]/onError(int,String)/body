{
  Log.debug("IRC error: " + i + ", "+ string);
  if (silenced) {
    return;
  }
  getSession().getTransport().sendMessage(getSession().getJID(),getSession().getTransport().getJID(),"IRC error received: (code " + i + ") "+ string,Message.Type.error);
}
