{
  if (minVolume >= falloff) {
    maxExp=0;
  }
 else {
    maxExp=(int)((Math.log(minVolume) / Math.log(falloff)));
  }
  if (Logger.logLevel >= Logger.LOG_INFO) {
    Logger.println("minVolume " + minVolume + " falloff "+ falloff+ " maxExp set to "+ maxExp);
  }
}
