{
  if (enabled) {
    System.out.println(prefix + " - SENT (" + session.hashCode()+ "): "+ Charset.forName("UTF-8").decode(((ByteBuffer)message).asReadOnlyBuffer()));
  }
  super.messageSent(nextFilter,session,message);
}
