{
  char[] ca=input.toCharArray();
  int len=ca.length;
  StringBuffer sb=new StringBuffer(len);
  sb.append('+');
  for (int i=0; i < len; ++i) {
    char c=ca[i];
    if (Character.isDigit(c)) {
      sb.append(c);
    }
  }
  return sb.toString();
}
