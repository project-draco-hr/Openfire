{
  char[] ca=key.toCharArray();
  int len=ca.length;
  StringBuffer sb=new StringBuffer(len * 2 + suffix.length());
  for (int i=len - 1; i > 0; --i) {
    sb.append(ca[i]);
    sb.append('.');
  }
  sb.append(suffix);
  return sb.toString();
}
