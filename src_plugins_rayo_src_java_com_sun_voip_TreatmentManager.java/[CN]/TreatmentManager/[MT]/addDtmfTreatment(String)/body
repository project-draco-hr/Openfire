{
  String s=treatment.replaceAll("[ \t]","");
  for (int i=0; i < s.length(); i++) {
    if (s.charAt(i) == ',') {
      addSilence();
    }
 else {
      addOneDtmfTreatment(s.charAt(i));
    }
  }
}
