{
  String msg=" A new user with the username of '" + user.getUsername() + "' just registered";
  router.route(createServerMessage(getContact() + "@" + serverName,"Registration Notification",msg));
}
