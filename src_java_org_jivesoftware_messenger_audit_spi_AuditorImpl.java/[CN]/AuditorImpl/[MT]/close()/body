{
  if (xmlWriter != null) {
    try {
      xmlWriter.flush();
      xmlWriter.close();
      writer.write("</jive>");
      writer.close();
      writer=null;
    }
 catch (    Exception e) {
      Log.error(LocaleUtils.getLocalizedString("admin.error"),e);
    }
  }
}
