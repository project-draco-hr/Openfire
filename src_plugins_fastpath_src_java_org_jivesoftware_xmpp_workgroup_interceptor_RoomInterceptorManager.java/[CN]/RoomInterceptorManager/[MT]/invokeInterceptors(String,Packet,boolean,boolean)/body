{
  try {
    super.invokeInterceptors(workgroup,packet,read,processed);
  }
 catch (  PacketRejectedException e) {
    ComponentManagerFactory.getComponentManager().getLog().error("Cannot reject " + "room packet",e);
  }
}
