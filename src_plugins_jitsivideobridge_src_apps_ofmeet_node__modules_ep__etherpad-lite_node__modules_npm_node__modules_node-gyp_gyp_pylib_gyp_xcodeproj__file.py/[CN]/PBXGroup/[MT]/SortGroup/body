def SortGroup(self):
    self._properties['children'] = sorted(self._properties['children'], cmp=(lambda x, y: x.Compare(y)))
    for child in self._properties['children']:
        if isinstance(child, PBXGroup):
            child.SortGroup()
