{
  ArrayList list=new ArrayList();
  FBGenericNACK cnack;
  for (Iterator i$=__genericNACKs.iterator(); i$.hasNext(); ArrayListExtensions.addRange(list,cnack.getBytes()))   cnack=(FBGenericNACK)i$.next();
  super.setFCIPayload((Byte[])list.toArray(new Byte[0]));
}
