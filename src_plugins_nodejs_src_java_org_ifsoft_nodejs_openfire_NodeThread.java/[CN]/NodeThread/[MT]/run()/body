{
  Log.info("Start run()");
  while (thread != null && thread.isAlive()) {
    try {
      String line=input.readLine();
      while (line != null) {
        Log.info("Node: " + line);
        line=input.readLine();
      }
      Thread.sleep(500);
    }
 catch (    Throwable t) {
    }
  }
}
