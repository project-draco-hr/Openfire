{
  if (OSGi.activator != null)   throw new IllegalStateException("activator");
  OSGi.activator=activator;
  if (launcher == null) {
    launcher=new OSGiLauncher(getBUNDLES());
  }
  launcher.start(activator);
}
