{
  String ldapURL="";
  try {
    ldapURL="ldap://" + host + ":"+ port+ "/"+ URLEncoder.encode(baseDN,"UTF-8");
    ldapURL=ldapURL.replaceAll("\\+","%20");
  }
 catch (  java.io.UnsupportedEncodingException e) {
    ldapURL="ldap://" + host + ":"+ port+ "/"+ baseDN;
  }
  return ldapURL;
}
