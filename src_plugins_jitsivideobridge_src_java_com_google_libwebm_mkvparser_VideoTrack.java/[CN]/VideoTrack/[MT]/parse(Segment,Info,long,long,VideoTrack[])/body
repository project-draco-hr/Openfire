{
  long[] jVideoTrack={0};
  long result=Parse(segment.getNativePointer(),info.getNativePointer(),element_start,element_size,jVideoTrack);
  videoTrack[0]=new VideoTrack(jVideoTrack[0]);
  return result;
}
