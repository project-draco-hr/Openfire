{
  if (JIVE_CONFIG_FILENAME == null) {
    throw new IllegalStateException("Need to set the configuration filename to use " + "before sending this message");
  }
  ;
  return JIVE_CONFIG_FILENAME;
}
