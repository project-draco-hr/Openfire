{
  List<Rule> aRules=lookupRules(aus,key);
  if (aRules == null) {
    return;
  }
  for (  Rule rule : aRules) {
    if (rule.isTerminal()) {
      rules.add(rule);
    }
 else {
      internalLookup(aus,rule.evaluate(),rules);
    }
  }
}
