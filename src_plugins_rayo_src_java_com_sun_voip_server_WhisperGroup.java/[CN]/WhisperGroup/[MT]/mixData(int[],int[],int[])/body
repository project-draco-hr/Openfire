{
  try {
    for (int i=0; i < outData.length; i++) {
      outData[i]=conferenceData[i] - memberData[i];
    }
  }
 catch (  IndexOutOfBoundsException e) {
    Logger.println("Exception!  conferenceData length " + conferenceData.length + " memberData.length "+ memberData.length+ " outData length "+ outData.length);
    e.printStackTrace();
  }
}
