{
  if (stringComparison == StringComparison.CurrentCultureIgnoreCase || stringComparison == StringComparison.InvariantCultureIgnoreCase || stringComparison == StringComparison.OrdinalIgnoreCase)   return Integer.valueOf(str.toLowerCase().indexOf(s.toLowerCase()));
 else   return Integer.valueOf(str.indexOf(s));
}
