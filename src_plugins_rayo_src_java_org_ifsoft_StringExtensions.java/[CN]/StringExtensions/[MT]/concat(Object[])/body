{
  StringBuilder sb=new StringBuilder();
  Object arr$[]=strs;
  int len$=arr$.length;
  for (int i$=0; i$ < len$; i$++) {
    Object s=arr$[i$];
    sb.append(s.toString());
  }
  return sb.toString();
}
