{
  if (isEnabled()) {
    startService();
    if (isLocalEnabled()) {
      startLocalServer();
    }
  }
 else {
    XMPPServer.getInstance().getIQDiscoItemsHandler().removeServerItemsProvider(this);
  }
}
