{
  int size=0;
  size+=CacheSizes.sizeOfObject();
  size+=CacheSizes.sizeOfInt();
  size+=CacheSizes.sizeOfBoolean();
  if (jidValue != null) {
    size+=CacheSizes.sizeOfString(jidValue.toString());
  }
  if (groupValue != null) {
    size+=CacheSizes.sizeOfString(groupValue);
  }
  size+=CacheSizes.sizeOfBoolean();
  size+=CacheSizes.sizeOfBoolean();
  size+=CacheSizes.sizeOfBoolean();
  size+=CacheSizes.sizeOfBoolean();
  size+=CacheSizes.sizeOfBoolean();
  return size;
}
