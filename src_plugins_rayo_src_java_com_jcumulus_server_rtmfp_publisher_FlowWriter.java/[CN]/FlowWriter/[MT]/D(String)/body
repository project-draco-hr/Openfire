{
  G.warn((new StringBuilder()).append("FlowWriter ").append(J).append(" has failed : ").append(s).toString());
  N=L=M=D=0;
  I();
  if (B) {
    return;
  }
 else {
    H.B(this);
    return;
  }
}
