{
  if (i >= D()) {
    G.error((new StringBuilder()).append("Impossible to cancel ").append(i).append(" message on flowWriter ").append(J).toString());
    return;
  }
synchronized (P) {
    for (int j=i; j < P.size(); j++)     P.remove(i);
  }
}
