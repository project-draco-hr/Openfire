{
  H=Integer.valueOf(0);
  if (!E) {
    if (B) {
      F.B((byte)11);
      F.A((double)b.getTime() / 1000D);
      F.A((short)0);
      return;
    }
    F.B((byte)17);
  }
  F.B((byte)8);
  F.B((byte)1);
  F.A((double)b.getTime() / 1000D);
  A.add(Integer.valueOf(8));
  H=Integer.valueOf(A.size());
}
