{
  H=Integer.valueOf(0);
  if (!E) {
    if (B && !flag) {
      G.push(Integer.valueOf(0));
      if (Strings.isNullOrEmpty(s)) {
        F.B((byte)3);
      }
 else {
        F.B((byte)16);
        A(s);
      }
      return;
    }
    F.B((byte)17);
    E=true;
  }
  F.B((byte)10);
  A.add(Integer.valueOf(10));
  H=Integer.valueOf(A.size());
  G.push(H);
  int i=1;
  i|=2;
  if (flag)   i|=4;
 else   i|=8;
  F.D(i);
  B(s);
}
