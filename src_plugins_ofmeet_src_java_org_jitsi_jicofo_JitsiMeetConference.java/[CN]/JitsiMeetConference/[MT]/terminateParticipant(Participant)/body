{
  JingleSession session=participant.getJingleSession();
  if (session != null) {
    jingle.terminateSession(session,Reason.EXPIRED);
  }
  chatRoom.kickParticipant(participant.getChatMember(),"End of the conference");
}
