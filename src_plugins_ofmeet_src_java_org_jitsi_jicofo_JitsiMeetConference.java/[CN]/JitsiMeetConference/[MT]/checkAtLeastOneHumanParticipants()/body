{
  int humanCount=0;
  for (  Participant participant : participants) {
    if (!isFocusMember(participant.getChatMember()) && !participant.isSipGateway())     humanCount++;
  }
  return humanCount > 0;
}
