{
  if (!started)   return;
  getDirectXmppOpSet().removePacketHandler(messageListener);
  disposeConference();
  leaveTheRoom();
  disposeAccount();
  listener.conferenceEnded(this);
  started=false;
}
