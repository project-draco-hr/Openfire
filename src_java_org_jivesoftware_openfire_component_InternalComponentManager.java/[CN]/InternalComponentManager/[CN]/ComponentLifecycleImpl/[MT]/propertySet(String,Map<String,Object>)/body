{
  if (property.equals(jiveProperty)) {
    boolean enabled=Boolean.FALSE.toString().equals(params.get("value"));
    if (enabled) {
      startComponent();
    }
 else {
      stopComponent();
    }
  }
}
