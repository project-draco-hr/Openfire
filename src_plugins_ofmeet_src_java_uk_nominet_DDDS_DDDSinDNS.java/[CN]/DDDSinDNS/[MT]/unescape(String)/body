{
  char[] c=input.toCharArray();
  StringBuffer sb=new StringBuffer(input.length());
  for (int i=0; i < c.length; ++i) {
    if (c[i] == '\\') {
      i++;
    }
    sb.append(c[i]);
  }
  return sb.toString();
}
