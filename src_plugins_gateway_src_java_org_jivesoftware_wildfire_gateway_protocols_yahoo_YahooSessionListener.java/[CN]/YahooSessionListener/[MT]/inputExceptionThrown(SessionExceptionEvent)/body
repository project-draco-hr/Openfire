{
  Log.error("Input error from yahoo: " + event.getMessage(),event.getException());
  yahooSession.getTransport().sendMessage(yahooSession.getJIDWithHighestPriority(),yahooSession.getTransport().getJID(),"Input error from yahoo: " + event.getMessage(),Message.Type.error);
}
