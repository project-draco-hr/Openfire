{
  if (event.getMailCount() > 0) {
    yahooSession.getTransport().sendMessage(yahooSession.getJIDWithHighestPriority(),yahooSession.getTransport().getJID(),"You have " + event.getMailCount() + " message(s) waiting in your Yahoo! mail.",Message.Type.headline);
  }
}
