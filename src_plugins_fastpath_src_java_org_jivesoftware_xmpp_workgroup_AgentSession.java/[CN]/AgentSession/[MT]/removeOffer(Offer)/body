{
  if (offer.equals(this.offer)) {
    this.offer=null;
  }
 else {
    ComponentManagerFactory.getComponentManager().getLog().debug("Offer not removed. " + "To remove: " + offer + " existing "+ this.offer);
  }
}
