{
  StringBuffer stringbuffer=new StringBuffer();
  stringbuffer.append(namePair);
  if (!parameters.isEmpty())   stringbuffer.append(";").append(parameters.encode());
  return stringbuffer.toString();
}
