def uniquer(seq, idfun=None):
    if (idfun is None):
        idfun = (lambda x: x)
    seen = {}
    result = []
    for item in seq:
        marker = idfun(item)
        if (marker in seen):
            continue
        seen[marker] = 1
        result.append(item)
    return result
