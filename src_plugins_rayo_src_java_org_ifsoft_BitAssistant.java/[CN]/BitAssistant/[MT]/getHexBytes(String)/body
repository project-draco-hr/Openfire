{
  int len=s.length();
  Byte bytes[]=new Byte[len / 2];
  for (int i=0; i < len; i+=2)   bytes[i / 2]=Byte.valueOf((byte)((Character.digit(s.charAt(i),16) << 4) + Character.digit(s.charAt(i + 1),16)));
  return bytes;
}
