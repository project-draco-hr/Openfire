{
synchronized (members) {
    Logger.println("addMember " + memberReceiver + " "+ memberReceiver.getMember().getMemberSender().getSendAddress());
    members.put(memberReceiver.getMember().getMemberSender().getSendAddress(),memberReceiver);
  }
}
