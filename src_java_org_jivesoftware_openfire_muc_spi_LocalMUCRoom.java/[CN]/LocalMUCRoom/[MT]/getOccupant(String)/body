{
  if (nickname == null) {
    throw new UserNotFoundException();
  }
  MUCRole role=occupants.get(nickname.toLowerCase());
  if (role != null) {
    return role;
  }
  throw new UserNotFoundException();
}
