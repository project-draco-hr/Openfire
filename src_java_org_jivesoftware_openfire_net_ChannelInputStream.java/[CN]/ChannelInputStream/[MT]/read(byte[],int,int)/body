{
  if (buf.position() == 0) {
    doRead();
  }
 else {
    buf.flip();
  }
  len=Math.min(len,buf.remaining());
  if (len == 0) {
    return -1;
  }
  buf.get(bytes,off,len);
  if (buf.hasRemaining()) {
    buf.compact();
  }
 else {
    buf.clear();
  }
  return len;
}
