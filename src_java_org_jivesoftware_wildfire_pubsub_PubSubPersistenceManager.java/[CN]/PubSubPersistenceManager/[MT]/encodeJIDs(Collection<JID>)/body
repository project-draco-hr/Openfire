{
  StringBuilder sb=new StringBuilder(90);
  for (  JID jid : jids) {
    sb.append(jid.toString()).append(",");
  }
  if (!jids.isEmpty()) {
    sb.setLength(sb.length() - 1);
  }
  return sb.toString();
}
