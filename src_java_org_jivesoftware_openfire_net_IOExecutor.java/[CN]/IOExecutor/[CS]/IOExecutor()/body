{
  int poolSize=JiveGlobals.getIntProperty("tiscali.pool.size",15);
  executeQueue=new LinkedBlockingQueue<>(10000);
  executeMsgPool=new ThreadPoolExecutor(poolSize,poolSize,60,TimeUnit.SECONDS,executeQueue);
}
