{
  List<String> list=new ArrayList<>();
  @SuppressWarnings("unchecked") List<Node> nodes=(List<Node>)element.selectNodes("return");
  for (  Node node : nodes) {
    list.add(node.getText());
  }
  return list;
}
