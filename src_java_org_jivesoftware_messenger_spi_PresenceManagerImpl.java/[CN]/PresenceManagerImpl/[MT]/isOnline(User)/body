{
  if (user == null) {
    return false;
  }
  Presence presence=(Presence)onlineUserCache.get(new Long(user.getID()));
  if (presence != null) {
    return presence.isAvailable();
  }
  return false;
}
