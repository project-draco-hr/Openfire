{
  lock.readLock().lock();
  try {
    String answer=members.get(bareJID);
    if (answer == null || answer.trim().length() == 0) {
      return null;
    }
    return answer;
  }
  finally {
    lock.readLock().unlock();
  }
}
