{
  Log.debug(event.toString());
  if (event.getType().equals(StatusConstants.NOTIFY_TYPING)) {
    yahooSession.getTransport().sendComposingNotification(yahooSession.getJIDWithHighestPriority(),yahooSession.getTransport().convertIDToJID(event.getFrom()));
  }
}
