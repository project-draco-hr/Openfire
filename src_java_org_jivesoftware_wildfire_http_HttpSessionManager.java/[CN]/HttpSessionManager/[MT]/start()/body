{
  inactivityThread=new HttpSessionReaper();
  inactivityTimer=new Timer("HttpSession Inactivity Timer");
  inactivityTimer.schedule(inactivityThread,30 * JiveConstants.SECOND,30 * JiveConstants.SECOND);
}
