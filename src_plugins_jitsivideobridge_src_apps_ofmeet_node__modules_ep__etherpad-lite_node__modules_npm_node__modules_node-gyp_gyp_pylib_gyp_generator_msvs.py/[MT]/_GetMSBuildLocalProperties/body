def _GetMSBuildLocalProperties(msbuild_toolset):
    properties = {}
    if msbuild_toolset:
        properties = [['PropertyGroup', {'Label': 'Locals', }, ['PlatformToolset', msbuild_toolset]]]
    return properties
