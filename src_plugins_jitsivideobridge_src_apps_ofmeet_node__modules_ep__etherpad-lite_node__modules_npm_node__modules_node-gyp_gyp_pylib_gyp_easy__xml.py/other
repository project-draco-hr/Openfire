import re
import os
_xml_escape_map = {'"': '&quot;', "'": '&apos;', '<': '&lt;', '>': '&gt;', '&': '&amp;', '\n': '&#xA;', '\r': '&#xD;', }
_xml_escape_re = re.compile(('(%s)' % '|'.join(map(re.escape, _xml_escape_map.keys()))))
