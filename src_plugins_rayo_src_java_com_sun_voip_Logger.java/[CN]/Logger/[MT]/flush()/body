{
  try {
    if (bw != null) {
      bw.flush();
    }
    if (fw != null) {
      fw.flush();
    }
  }
 catch (  IOException e) {
    System.out.println(getDate() + "could not flush log file. " + e.getMessage());
  }
}
