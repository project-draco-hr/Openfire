{
  if (Logger.logLevel >= Logger.LOG_INFO) {
    Logger.writeFile(" RTCP Receiver report:  " + " from " + from + ", fractionLost "+ getFractionLost()+ ", highest sequence "+ Integer.toHexString(getHighestSeqReceived())+ ", cumulativeLost "+ getCumulativeLost()+ ", jitter "+ getInterArrivalJitter()+ " LSR "+ getLSR());
  }
}
