{
  Log.debug(getType().toString() + ": addingContact " + roster.getUsername()+ ":"+ item.getJid()+ " is "+ persistent);
  return !(item.getJid().getDomain().equals(this.getJID()) && item.getJid().getNode() != null) && persistent;
}
