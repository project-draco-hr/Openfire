{
  if (Logger.logLevel >= Logger.LOG_INFO) {
    Logger.writeFile(" RTCP Sender report:  from " + from + ", RTP Timestamp "+ Integer.toHexString(getRTPTimestamp())+ ", packets "+ getPacketCount()+ ", octets "+ getOctetCount()+ ", fractionLost "+ getFractionLost()+ ", cumulativeLost "+ getCumulativeLost()+ ", highest seq "+ Integer.toHexString(getHighestSeqReceived())+ ", jitter "+ getInterArrivalJitter());
  }
}
