{
  if (!running) {
    return;
  }
  try {
    componentManager.removeComponent(this.type.toString());
  }
 catch (  Exception e) {
    componentManager.getLog().error(e);
  }
  transport=null;
  running=false;
}
