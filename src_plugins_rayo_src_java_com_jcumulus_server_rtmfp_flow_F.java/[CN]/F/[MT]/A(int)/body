{
  H=Integer.valueOf(0);
  if (!E) {
    if (B) {
      A(i);
      return;
    }
    F.B((byte)17);
  }
  F.B((byte)4);
  if (i > 0xfffffff) {
    D.error("AMF Integer maximum value reached");
    i=0xfffffff;
  }
 else   if (i < 0)   i+=0x20000000;
  F.D(i);
}
