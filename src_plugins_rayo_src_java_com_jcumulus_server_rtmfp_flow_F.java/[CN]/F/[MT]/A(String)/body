{
  H=Integer.valueOf(0);
  if (Strings.isNullOrEmpty(s)) {
    F.B(((byte)(E ? 1 : 6)));
    return;
  }
  if (!E) {
    if (B) {
      if (s.length() > 65535) {
        F.B((byte)12);
        F.A(s.length());
        F.A(s);
      }
 else {
        F.B((byte)2);
        F.D(s.getBytes());
      }
      return;
    }
    F.B((byte)17);
  }
  F.B((byte)6);
  E(s);
  H=Integer.valueOf(A.size());
}
