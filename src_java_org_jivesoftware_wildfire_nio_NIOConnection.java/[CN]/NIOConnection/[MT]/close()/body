{
  boolean wasClosed=false;
synchronized (this) {
    if (!isClosed()) {
      try {
        deliverRawText("</stream:stream>");
      }
 catch (      Exception e) {
      }
      closeConnection();
      wasClosed=true;
    }
  }
  if (wasClosed) {
    notifyCloseListeners();
  }
}
